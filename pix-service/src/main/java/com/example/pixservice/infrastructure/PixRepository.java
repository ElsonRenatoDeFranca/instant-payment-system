package com.example.pixservice.infrastructure;

import com.example.pixservice.domain.PixTransaction;
import org.springframework.stereotype.Repository;

@Repository
public class PixRepository {
    // Example stub
    public PixTransaction save(PixTransaction tx) {
        // Simulate save
        return tx;
    }
}

